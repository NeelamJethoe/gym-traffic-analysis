# Gym Traffic Analysis
TBD

## Data source
The raw source file is not included in this repository for privacy reasons.

## Power Query setup
The Power Query source uses a parameter called `pFilePath`.

Before refreshing the queries, update `pFilePath` to the local path of the source Excel file.

## Status
Work in progress. Current phase: data preparation, deduplication, and base traffic analysis.
